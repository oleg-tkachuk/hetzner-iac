{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Chart pins live in Go rather than a Chart.yaml, so a regex manager does the work no built-in manager can.",
    "rebaseWhen keeps branches current because protection on main is strict: without it every pull request goes stale as soon as anything else lands, and has to be rebased by hand.",
    "The limits exist because twelve charts, a Go module graph and the pinned actions all come due on the first run, and a flood of pull requests is a flood nobody reads.",
    "Renovate runs from .github/workflows/renovate.yaml, not the hosted app. That cron decides how often it runs; the schedule here decides what it may do when it runs. Regular updates wait for Monday so chart upgrades batch into one review; vulnerabilityAlerts below are exempt and can land on any daily pass.",
    "A manual workflow_dispatch sets RENOVATE_FORCE and ignores the schedule below, which is how the first pass happens without waiting for Monday."
  ],
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":dependencyDashboard"
  ],
  "timezone": "Europe/Kyiv",
  "rebaseWhen": "behind-base-branch",
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "schedule": ["before 09:00 on monday"],

  "vulnerabilityAlerts": {
    "description": "Security fixes ignore the schedule and the concurrency limits.",
    "schedule": ["at any time"],
    "labels": ["security"]
  },

  "customManagers": [
    {
      "description": "The chart registry. The pattern keys off the field order Name → Repo → Version, which every entry follows; a test in tools/charts reads this very regex out of this file and asserts it still matches every chart, because the failure mode otherwise is Renovate silently opening no pull requests at all.",
      "customType": "regex",
      "managerFilePatterns": ["/^pkg/charts/registry\\.go$/"],
      "matchStrings": [
        "Name:\\s+\"(?<depName>[^\"]+)\",\\s*\\n\\s*Repo:\\s+\"(?<registryUrl>[^\"]+)\",\\s*\\n\\s*Version:\\s+\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "helm",
      "versioningTemplate": "helm"
    }
  ],

  "packageRules": [
    {
      "description": "A chart upgrade changes what this repository deploys, so it belongs in a release: semantic-release cuts a patch from fix.",
      "matchDatasources": ["helm"],
      "semanticCommitType": "fix",
      "semanticCommitScope": "charts",
      "labels": ["charts"],
      "additionalBranchPrefix": "chart-"
    },
    {
      "description": "A major chart jump becomes a minor release rather than hiding in a patch.",
      "matchDatasources": ["helm"],
      "matchUpdateTypes": ["major"],
      "semanticCommitType": "feat",
      "semanticCommitScope": "charts",
      "labels": ["charts", "major"]
    },
    {
      "description": "AppVersion beside each pin cannot be maintained by a bot: the helm datasource knows chart versions and nothing else. The Chart pins check fails these pull requests until a human sets it, which is also when the changelog gets read.",
      "matchDatasources": ["helm"],
      "prBodyNotes": [
        "`AppVersion` in `pkg/charts/registry.go` is **not** updated by this pull request, and the `Chart pins` check will fail until it is.",
        "Run `task charts:appversions` for the value to set, and update the `// app <version>` comment beside `Version` to match."
      ]
    },
    {
      "description": "Tooling and the module graph are not deployable changes, so chore keeps them out of the release notes.",
      "matchManagers": ["gomod", "github-actions"],
      "semanticCommitType": "chore",
      "semanticCommitScope": "deps"
    },
    {
      "description": "go mod tidy after every update. Without it go.sum is left missing entries for packages imported only behind a build tag — test/e2e is one, and the priming job's `go vet -tags e2e` is what found it.",
      "matchManagers": ["gomod"],
      "postUpdateOptions": ["gomodTidy"]
    },
    {
      "description": "The k8s.io and sigs.k8s.io modules are one matched set and must move together. k8s.io/api v0.37.0 removed packages that client-go v0.35.3 still imports, so a lone api bump does not compile — Renovate opened exactly that pull request, and it could never have gone green.",
      "matchManagers": ["gomod"],
      "matchPackageNames": ["k8s.io/**", "sigs.k8s.io/**"],
      "groupName": "kubernetes libraries"
    },
    {
      "description": "One pull request for the rest of the module graph's routine moves. Individually they are noise; together they are one review.",
      "matchManagers": ["gomod"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "go module graph"
    },
    {
      "description": "Actions are pinned to commit shas with a version comment; Renovate maintains both.",
      "matchManagers": ["github-actions"],
      "pinDigests": true,
      "groupName": "github actions"
    }
  ]
}
