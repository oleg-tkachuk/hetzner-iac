{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Chart pins live in Go rather than a Chart.yaml, so a regex manager does the work no built-in manager can.",
    "rebaseWhen keeps branches current because protection on main is strict: without it every pull request goes stale as soon as anything else lands, and has to be rebased by hand.",
    "The limits exist because twelve charts, a Go module graph and the pinned actions all come due on the first run, and a flood of pull requests is a flood nobody reads.",
    "Renovate runs from .github/workflows/renovate.yaml, not the hosted app. That cron decides how often it runs; the schedule here decides what it may do when it runs. Regular updates wait for Monday so chart upgrades batch into one review; vulnerabilityAlerts below are exempt and can land on any daily pass.",
    "The schedule is the WHOLE of Monday, and the width is the point. It was 'before 09:00 on monday' — which with timezone Europe/Kyiv is Sunday 21:00 to Monday 06:00 UTC, and the workflow's cron fires at 06:00 UTC, exactly as that window shuts. GitHub runs scheduled workflows best-effort on shared runners: the Monday pass observed started at 11:49 UTC, five hours and forty-nine minutes late, found three updates and put all three under 'Awaiting Schedule'. Renovate had opened no pull request in this repository, ever. Batching into one weekly review is served by 'Monday'; the hour added nothing and made the whole thing depend on a cron being punctual.",
    "A manual workflow_dispatch sets RENOVATE_FORCE and ignores the schedule below, which is how the first pass happens without waiting for Monday.",
    "The ansible manager is off because it matched tasks/cluster.task.yaml and extracted nothing. Renovate's ansible manager reads files under a tasks/ directory — an Ansible role convention — and looks for an 'image:' key, which the task named 'image:bake' provides. This repository's tasks/ holds Task taskfiles, so every hit there is a false one. Disabled by name rather than through enabledManagers: an allow-list has to be extended for each new manager, and forgetting is silent."
  ],
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":dependencyDashboard"
  ],
  "timezone": "Europe/Kyiv",
  "rebaseWhen": "behind-base-branch",
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "schedule": ["* * * * 1"],

  "ansible": {
    "enabled": false
  },

  "vulnerabilityAlerts": {
    "description": "Security fixes ignore the schedule and the concurrency limits.",
    "schedule": ["at any time"],
    "labels": ["security"]
  },

  "customManagers": [
    {
      "description": "The chart registry. The pattern keys off the field order Name → Repo → Version, which every entry follows; a test in tools/charts reads this very regex out of this file and asserts it still matches every chart, because the failure mode otherwise is Renovate silently opening no pull requests at all.",
      "customType": "regex",
      "managerFilePatterns": ["/^internal/pkg/charts/registry\\.go$/"],
      "matchStrings": [
        "Name:\\s+\"(?<depName>[^\"]+)\",\\s*\\n\\s*Repo:\\s+\"(?<registryUrl>[^\"]+)\",\\s*\\n\\s*Version:\\s+\"(?<currentValue>[^\"]+)\""
      ],
      "datasourceTemplate": "helm",
      "versioningTemplate": "helm"
    },
    {
      "description": "Pinned tool versions in the workflows. Nine of them, across ci.yaml and security.yaml, with mixed datasources — GitHub releases, PyPI and Go modules — so the `renovate:` comment above each pin carries the datasource and depName rather than this pattern knowing nine special cases. extractVersion appears on the pins that omit the `v` their upstream tag carries. A test in internal/ci reads this regex out of this file and asserts it still matches every annotated pin AND that every pin is annotated, because the failure mode is the same one the chart manager has: Renovate opens no pull request, for ever, and nothing says so.",
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/[^/]+\\.yaml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+?)(?: versioning=(?<versioning>\\S+?))?(?: extractVersion=(?<extractVersion>\\S+?))?\\s+[A-Z0-9_]+_VERSION:\\s*\"(?<currentValue>[^\"]+)\""
      ]
    }
  ],

  "packageRules": [
    {
      "description": "A chart upgrade changes what this repository deploys, so it belongs in a release: semantic-release cuts a patch from fix.",
      "matchDatasources": ["helm"],
      "semanticCommitType": "fix",
      "semanticCommitScope": "charts",
      "labels": ["charts"],
      "additionalBranchPrefix": "chart-"
    },
    {
      "description": "A major chart jump becomes a minor release rather than hiding in a patch.",
      "matchDatasources": ["helm"],
      "matchUpdateTypes": ["major"],
      "semanticCommitType": "feat",
      "semanticCommitScope": "charts",
      "labels": ["charts", "major"]
    },
    {
      "description": "AppVersion beside each pin cannot be maintained by a bot: the helm datasource knows chart versions and nothing else. The Chart pins check fails these pull requests until a human sets it, which is also when the changelog gets read.",
      "matchDatasources": ["helm"],
      "prBodyNotes": [
        "`AppVersion` in `internal/pkg/charts/registry.go` is **not** updated by this pull request, and the `Chart pins` check will fail until it is.",
        "Run `task charts:appversions` for the value to set, and update the `// app <version>` comment beside `Version` to match."
      ]
    },
    {
      "description": "Tooling and the module graph are not deployable changes, so chore keeps them out of the release notes.",
      "matchManagers": ["gomod", "github-actions"],
      "semanticCommitType": "chore",
      "semanticCommitScope": "deps"
    },
    {
      "description": "The pinned CI tools, same reasoning: they change what the checks are, never what is deployed. Scoped by datasource rather than by manager, because the other custom manager is the chart registry and its pull requests are releases.",
      "matchManagers": ["custom.regex"],
      "matchDatasources": ["github-releases", "pypi", "go"],
      "semanticCommitType": "chore",
      "semanticCommitScope": "ci",
      "labels": ["ci"],
      "groupName": "ci tooling"
    },
    {
      "description": "go mod tidy after every update. Without it go.sum is left missing entries for packages imported only behind a build tag — test/e2e is one, and the priming job's `go vet -tags e2e` is what found it.",
      "matchManagers": ["gomod"],
      "postUpdateOptions": ["gomodTidy"]
    },
    {
      "description": "The k8s.io and sigs.k8s.io modules are one matched set and must move together. k8s.io/api v0.37.0 removed packages that client-go v0.35.3 still imports, so a lone api bump does not compile — Renovate opened exactly that pull request, and it could never have gone green. The same class from the other direction, measured since: k8s.io/kube-openapi has no releases at all, so it resolves to a pseudo-version of its master — which requires sigs.k8s.io/structured-merge-diff/v7 while the newest RELEASED apimachinery still uses /v6, and apimachinery converts a kube-openapi schema with it. `go get -u ./...` produced exactly that split and the build failed inside apimachinery, naming neither the upgrade nor the reason. This group is what keeps the two in step; the pattern below covers both, which is why no separate check is needed for it.",
      "matchManagers": ["gomod"],
      "matchPackageNames": ["k8s.io/**", "sigs.k8s.io/**"],
      "groupName": "kubernetes libraries"
    },
    {
      "description": "One pull request for the rest of the module graph's routine moves. Individually they are noise; together they are one review.",
      "matchManagers": ["gomod"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "go module graph"
    },
    {
      "description": "Actions are pinned to commit shas with a version comment; Renovate maintains both. Scoped to depType `action` — the `uses:` reference itself — because the same manager also reports a step's INPUTS as dependencies, with depType `uses-with`, and a version input has no digest to pin. Pinning the whole manager made Renovate try to write helm/helm's commit sha into azure/setup-helm's `version:` input, fail to read it back, and report `Error updating branch: update failure` on the dashboard on every run.",
      "matchManagers": ["github-actions"],
      "matchDepTypes": ["action"],
      "pinDigests": true,
      "groupName": "github actions"
    }
  ]
}
